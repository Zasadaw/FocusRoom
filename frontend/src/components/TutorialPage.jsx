export default function TutorialPage() {
  const items = [
    ["Регистрация", "Для создания аккаунта нужны логин, email и пароль. Кодовое слово можно придумать по желанию — оно понадобится только для восстановления доступа."],
    ["Комнаты", "Создатель комнаты получает роль owner. Другие пользователи заходят только по коду комнаты и паролю, если он задан."],
    ["Доступ", "В разделе комнат показываются только те комнаты, где пользователь уже является участником."],
    ["Обновление без перезагрузки", "После создания комнаты, входа, отправки сообщений и редактирования данные сразу перечитываются с сервера."],
    ["Админ-панель", "Открывается отдельно на порту 8001. Логин и пароль по умолчанию: root / root."],
    ["Публикация", "Для общего доступа запускайте backend на 0.0.0.0 и используйте HTTPS на сервере или через прокси."],
  ];
  return (
    <section className="card">
      <h2>Как пользоваться сайтом</h2>
      <div className="tutorial-list">
        {items.map(([title, text], index) => (
          <article className="tutorial-step" key={title}>
            <strong>{index + 1}</strong>
            <div><h3>{title}</h3><p>{text}</p></div>
          </article>
        ))}
      </div>
    </section>
  );
}
